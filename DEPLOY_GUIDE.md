# Como Publicar na Vercel 🚀

Seu código já está no GitHub. Agora vamos colocar o site no ar usando a Vercel.
É gratuito e faz o processo automaticamente.

## Passo 1: Criar Conta
1. Acesse [vercel.com](https://vercel.com/signup).
2. Escolha **"Continue with GitHub"** (Isso é importante para ele acessar seu código).

## Passo 2: Importar o Projeto
1. No seu painel (Dashboard), clique no botão branco **"Add New..."** e depois em **"Project"**.
2. Na lista "Import Git Repository", você deve ver o seu projeto `sindinor`.
3. Clique no botão **"Import"** ao lado dele.

## Passo 3: Configurar e Publicar
1. Ele vai mostrar uma tela de configuração ("Configure Project").
   - **Framework Preset**: Ele deve detectar `Vite` automaticamente. Se não, selecione `Vite`.
   - **Root Directory**: Deixe como `.` (padrão) ou `sindinor` (se o código estiver dentro de uma pasta).
2. Clique no botão azul **"Deploy"**.

## Passo 4: Pronto!
1. Aguarde cerca de 1 a 2 minutos enquanto ele constrói o site.
2. Quando terminar, vai aparecer uma tela festiva com confetes 🎉.
3. Clique na imagem do site ou em **"Visit"**.
4. Este é o seu link oficial (algo como `sindinor.vercel.app`). Você pode compartilhar esse link com todos!

---

**Observação:** Toda vez que você ou eu enviarmos uma alteração para o GitHub (como acabamos de fazer), a Vercel vai detectar e atualizar o site sozinha em poucos minutos. Você não precisa fazer esse processo de novo.
